#ifndef ALLLIB_H_INCLUDED
#define ALLLIB_H_INCLUDED
    
    #define EMPTY -10000
    #define PLAYER (EMPTY - 1)
    #define COMPUTER (EMPTY - 2)
    #define DRAW (EMPTY - 3)
    #define RUNNING (EMPTY - 4)
    #define HINT (EMPTY - 5)
    #define ROW 7
    #define COL 7
    
    #include "game.h"
    #include "calc.h"
    #include "analyse.h"
    #include "input.h"
    #include "paint.h"
    #include <stdio.h>
    #include <stdlib.h>
    #include <time.h>

#endif